# Auto detect text files and perform LF normalization
* text=auto

# 셸 스크립트는 항상 LF로 (WSL bash가 실행 — CRLF면 'set -o pipefail'·heredoc·'exit 0'이 깨져
# app:bake 실패, guard/enforce 훅이 오작동(판정 뒤집힘). 플랫폼 무관하게 LF 강제.
*.sh text eol=lf
