# 0801 PokemonWith 작업일지

> 이어받는 다음 세션(다른 PC 포함)이 **이 문서만 보고** 이어가게 쓴다.
> 이 세션 = ① 0728이 남긴 **스토리 봉합**("이상한 꿈") 구현 → ② 사용자 질문 "**날씨·밤낮·시간 흐름 다 적용했어?**"
> → **아니오(하나도 없었음)** → 밤낮 시스템 신규 구현 → ③ 사용자 지시로 **AR `Graphics/` 전수 조사**.

---

## 0. ⭐⭐ 가장 중요 — 놓친 것 목록 (사용자: "다 해놔야 할 걸로 적어놔, 심각해")

> 원본 `Graphics/` 폴더를 다 열어보니 **폴더 자체가 원본의 기능 목록**이었다.
> 우리가 안 만든 게 아니라 **있는 줄도 몰랐던 것들**이다. 아래는 전수 조사 결과 = **앞으로 반드시 할 일**.
> 조사 경로(집 PC): `C:\Users\ONE\Desktop\Pokemon Another Red_PWT_250829\Graphics`

| 폴더 | 원본 | 정체 | 우리 상태 | 우선순위 |
|---|---|---|---|---|
| **Weather** | 23장 | 비·폭풍·눈보라·모래폭풍·안개 오버레이(필드 날씨) | ❌ 없음 | **1** (밤낮 다음) |
| **Battlebacks** | 170장 (**`_eve`/`_night` 42장**) | 배틀 배경 + **시간대별 그림** | 낮 3종 + **이번에 route/town 저녁·밤 4장 이식** | ✅ 부분완료 |
| **Transitions** | 88장 (**`hgss_vsBar_LEADER_*`, `hgss_vs1/2`**) | 배틀 진입 전환 + **체육관장·챔피언 VS 연출** | ❌ 없음 (그린전이 그냥 페이드) | **2** (임팩트 큼) |
| **Autotiles** | 58장 | **물결·용암 애니 타일**(원본은 물이 움직인다) | ❌ 없음 — 우리 맵은 통짜 PNG라 물이 정지 | **3** |
| **Pictures** | 59장 (`Map07`, `mapRegion0`, `introBoy`) | **타운맵/지역맵** 그림 | ❌ 없음 (지도 기능 자체가 없음) | **4** |
| **Fogs** | 21장 | 안개·구름 오버레이(동굴·산) | ❌ 없음 | 5 |
| **Windowskins** | 107장 (`frlgtextskin`, `goldskin`, `sign hgss *`) | 대화창 스킨 + **표지판 창** | ❌ 없음 — 우리는 코드로 직접 그림 | 6 (look은 사용자 결정) |
| **Icons** | 17장 | 가방 포켓 아이콘 9 + 방향 화살표 8 | 부분 | 7 |
| **Plugins** | 205장 | Deluxe Battle Kit·Dynamax·Terastal·Z-Power·Enhanced UI 그래픽 | ❌ 없음 | 보류(스코프 밖) |
| Pokemon / Characters / Items / Animations / Trainers | 10300 / 3264 / 792 / 705 / 338 | 스프라이트 원본 | "쓸 것만 이식" 원칙대로 진행 중 | 정상 |

**결론: 진짜 구멍은 스프라이트가 아니라 '시스템이 통째로 없는 것'** — 날씨·VS연출·물 애니·타운맵.
(오늘 그중 **밤낮**을 메웠다.)

---

## 1. 밤낮(시간 흐름) — 신규 `src/systems/daynight.ts`

### 왜
사용자 질문 "날씨랑 밤낮 이런거 계속 바뀌고 시간 흐름같은 거 다 적용했어?" → `src/` 전체 grep 결과 **0건**.
**이 게임엔 시간 개념이 아예 없었다**(언제 켜도 대낮). 반면 원본은 배틀백에 `_eve`/`_night`가 42장 있다 = 원본은 시간대가 돈다.

### 어떻게 (⚠️ 사용자 지적 반영 — "해가 점점 지고 점점 뜨고 이렇게 될텐데")
- **구간(아침/낮/저녁/밤)으로 색을 툭툭 바꾸지 않는다.** `TONE_STOPS`(시각별 기준색 12개)를 두고
  **시:분을 실수 시각으로 보고 그 사이를 섞는다**(`toneAt`). 18시부터 20시까지 두 시간에 걸쳐 노을이 서서히 짙어진다.
- 검증 수치: **5분 사이 최대 변화 = alpha 0.0109 / 색 채널 4** → 눈에 안 띈다(끊김 없음).
- 구간(`TimeBand`)은 **HUD 글자와 배틀 배경 고르기에만** 쓴다(그림 파일은 원래 이산적이니까).
- 경계는 원본 엔진 기본값: 아침5~9 · 낮10~16 · 저녁17~19 · 밤20~4.
- **세이브에 시간을 저장하지 않는다** — 실제 시계(`new Date()`)를 읽을 뿐이라 저장할 게 없다(세이브 버전 안 올림).

### 붙인 곳
- `WorldScene`: `attachDayNight(this, 900, …)` — 화면 전체 색조 사각형(depth 900, HUD보다 위·대화창보다 아래).
  1분마다 지금 시각 색조를 다시 계산해 2초에 걸쳐 옮겨간다. 구간이 바뀌면 **HUD의 시간대 글자도 갈아끼운다**(`updateHudTime`).
  실내(InteriorScene)는 안 건다 — 원본도 실내는 조명이 있는 것으로 친다.
- `BattleScene`: `backdropName(backdrop, band)` → `route_bg / route_eve_bg / route_night_bg`(원본 파일명 규칙 그대로).
  ⚠️ 텍스처 키가 `bb_bg` 하나라 **매번 `textures.remove` 후 다시 로드**한다(안 그러면 저녁에 싸운 배경이 낮에도 남는다).
- 이식: `public/assets/battlebacks/{route,town}_{eve,night}_bg.png` 4장 (AR 원본 그대로 복사).

### 디버그(밤을 밤까지 기다릴 수 없으니)
`applyDebugBand(reg, data)` — 확인 항목이 `data:{debugTimeBand:"night"}`를 넘기면 강제.
⚠️ **값이 없으면 지운다** — 안 지우면 한 번 강제한 밤이 다른 항목·실제 플레이까지 따라다닌다.

---

## 2. 스토리 봉합 — "이상한 꿈"(0728 §5의 0번 그대로 구현)

| 곳 | 한 일 |
|---|---|
| `data/story.ts` | `momMorningLines()`(**사용자 원문 2줄**) · `momTalkLine()` · `oakLetterLines()` 앞에 **사용자 원문** `아 참, ○○! 어머니께 얘기는 들었다. 이상한 꿈을 꿨다고.` + `그 이야기라면 미리 준비해 둔 게 있단다.` |
| `InteriorScene.ts` | 목소리만 있던 **엄마를 1층 거실 NPC로 실체화**(AR `NPC_MidageWoman.png`). 계단으로 내려오면 다가와 말 걸고 → 부엌(13,4)으로 복귀. 이후 마주보고 Space = 말 걸기 |
| `systems/save.ts` | 옛 세이브 보정 — `starterChosen`은 있는데 소개장이 없으면 채워 넣는다(세이브 버전 안 올림) |
| `LabScene.ts` | 주석만: **파이리 `도롱뇽포켓몬`은 오타가 아니다**(아래) |

- **엄마 이동 경로는 좌표를 손으로 안 찍는다** — `npcPath()`(BFS)로 매번 계산. 격자가 바뀌어도 벽을 안 뚫는다(AGENTS.md 눈대중 금지).
- 발동 조건도 세이브 필드를 안 늘린다: `houseIntroDone && !starterChosen` + 씬 인스턴스 플래그(`momGreeted`, init에서 리셋).
- 엄마 칸(13,4)은 `walkable()`에서 막는다(사람이 서 있으니 못 지나감).
- **기존 대사(인트로 나레이션·엄마의 "어머, 왔니?"·네모 전부)는 한 줄도 안 건드렸다.**
- ⚠️ `momTalkLine`의 **스타터 수령 후 대사 `조심히 다녀오렴.` 한 줄만 내가 썼다**(사용자 원문 아님). 마음에 안 들면 이 한 줄만 갈아끼우면 된다.

### 파이리 분류명 (0728 §5의 0-2 — 결론: **고치지 말 것**)
웹 대조 결과 **포켓몬코리아 공식 도감 표기 = `도롱뇽포켓몬`**이 맞다.
일본판 `とかげ`(도마뱀)·영문 `Lizard`의 오역이지만 **공식 표기라 그대로 둔다**(AGENTS.md §1.5). `LabScene.ts:26`에 주석으로 못 박음.

---

## 3. 확인 항목(디버그 페이지) — 규칙대로 추가까지 완료

`src/data/debugChecks.ts` 맨 위 **0801 항목 5개**:
1. 밤낮 — 야외 밤 색조(`WorldScene`, `debugTimeBand:"night"`)
2. 밤낮 — 저녁 노을(`evening`)
3. 밤낮 — 배틀 배경도 시간대를 따라감(`BattleScene` + `night`)
4. 엄마 NPC 아침 대사(`InteriorScene`, `{room:"living", debugMomGreet:true}`)
5. 오박사 "어머니께 얘기는 들었다"(`LabScene`)

---

## 4. 검증 (headless, dev 5180) — 전부 통과·콘솔 에러 0·`tsc --noEmit` EXIT=0

- `tools/dbg-story-0801.mjs` — ① **계단으로 내려오는 실제 경로**에서 발동 ② 대사 2줄 원문 ③ 부엌 복귀 ④ 말 걸기 ⑤ 오박사 꿈 대사 + 소개장 지급 ⑥ 옛 세이브 보정.
- `tools/dbg-daynight-0801.mjs` — ① 색조 곡선 끊김 검사(5분당 최대 alpha 0.0109) ② 시각별 야외 캡처 10장 ③ 밤 배틀배경 ④ 확인항목 경로(HUD "밤", alpha 0.45).
- 캡처: `<리포루트>/.claude/.verify/0801_*.png` (+ `0801_밤낮_몽타주.png` = 04시~22시 10칸 비교).

### 못 한 것
- **exe 미반영**(`npm run app:bake` 안 함) · **`/code-review` 미실행**(슬래시=사용자 트리거).
- 실내(집·연구소·체육관)는 밤낮을 안 건다 — 창문 있는 방은 원본처럼 색조를 줄지 **사용자 결정 필요**.
- 날씨·VS연출·물 애니·타운맵 = §0 목록 그대로 남음.

---

## 5. 다음 세션에 할 일 (우선순위)

1. **⭐ 날씨 시스템** — `Graphics/Weather` 23장(비·폭풍·눈보라·모래폭풍·안개). 필드 오버레이 + 배틀 연동(날씨=데미지 보정)까지 갈지 먼저 정할 것.
2. **VS 연출** — `Graphics/Transitions`의 `hgss_vsBar_LEADER_Green` 등. 체육관장·라이벌 배틀 진입에 붙이면 임팩트가 크다.
3. **물 애니(Autotiles)** — 우리 맵이 통짜 PNG라 물이 안 움직인다. 물 타일 위치만 뽑아 애니 오버레이를 얹는 방식 검토.
4. **타운맵(Pictures)** — `mapRegion0.png` + `Map**.png`. 메뉴에 지도 항목.
5. 1번도로 누락 이벤트 배선(0728 이월) — **오박사 조우(31,22)**·필드아이템 3·열매나무 2·안내판.
6. 네모 유대 대사(SV 공식 대사 확보 후) · `app:bake` · `/code-review`.
7. 이월: 도감번호 `———`(SummaryScene) · Summary 빈 기술칸 · 라이벌집 마감(NPC·워프·매트).

---

## 6. 함정 (이번에 걸린 것)

1. **headless(swiftshader)는 ~3fps로 돈다** → 씬 타이머·트윈이 **실제 시간의 몇 배로 느리게** 흐른다.
   워프(fadeOut 300 + delayedCall 320)가 실시간 5초에도 안 끝나 "전환이 멈췄다"고 오진했다.
   → 검증 스크립트는 **고정 sleep 말고 `waitForFunction` + 타임아웃 90초**로.
2. **소개장은 대사보다 먼저 가방에 들어간다**(`addItem` → 그 다음 대사). 아이템만 보고 루프를 끊으면
   **정작 새로 넣은 대사가 한 줄도 안 지나간 채** 검증이 끝난다(실제로 첫 실행에서 그랬다).
3. **dev 서버가 조용히 죽어 있을 수 있다** — 스크립트가 `page.goto` 타임아웃이면 먼저 `ps aux | grep vite`.
   새 에셋(battlebacks 4장)을 넣었으면 **dev 재시작 필수**(0727·0728과 같은 함정).
4. WSL에서 `curl localhost:5180`이 응답 없이 멈추는 경우가 있다 — 서버는 멀쩡하다. **확인은 playwright로.**
5. 검증 `.mjs`는 `tools/` 안에서 실행(캡처 경로 `../../.claude/.verify`).

---

## 다음 세션 첫 프롬프트 (복붙용)

> "0801 일지 읽고 이어서. 스토리 봉합('이상한 꿈' — 1층 엄마 NPC·오박사 소개장 개연성·옛세이브 보정)과 **밤낮 시스템**(`src/systems/daynight.ts`, 시:분 연속 보간 + 시간대별 배틀백)은 구현·검증·커밋까지 끝났다(tsc 0, 콘솔에러 0, 캡처 확인).
>
> 오늘 1순위 = **일지 §0의 '놓친 것 목록' 중 날씨 시스템**. `C:\Users\ONE\Desktop\Pokemon Another Red_PWT_250829\Graphics\Weather`의 23장(비·폭풍·눈보라·모래폭풍·안개)을 이식해 야외에 붙인다. **밤낮과 같은 원칙: 세이브 필드 늘리지 말고, 확인 항목(debug)으로 날씨를 강제할 수 있게.** 배틀 데미지 보정까지 갈지는 먼저 물어볼 것.
> 2순위 = **VS 연출**(`Graphics/Transitions`의 `hgss_vsBar_LEADER_Green`·`hgss_vs1/2`) — 그린전·라이벌전 진입에.
> 3순위 = 1번도로 누락 이벤트 배선(오박사 조우 31,22 / 아이템 3 / 열매나무 2 / 안내판).
>
> ⚠️ headless는 3fps라 씬 타이머가 느리다 — 검증은 고정 sleep 말고 waitForFunction(타임아웃 90초). 검증 `.mjs`는 `myPokemon_AJ/tools/` 안에서 실행, 캡처는 `../../.claude/.verify`. 창 띄우기 전 먼저 물어볼 것."
