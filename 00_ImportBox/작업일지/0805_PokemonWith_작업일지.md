# 0805 PokemonWith 작업일지

> 이어받는 다음 세션(다른 PC 포함)이 **이 문서만 보고** 이어가게 쓴다.
> 이 세션 = 사용자 질문 *"지금 규칙이랑 세션 시작할 때 읽어들이는 것들, 양이 어떻고 공식 문서 봤을 때 성능 저하 우려 있어?"* 에서 시작한 **규칙·컨텍스트 정비**.

---

## 0. 한 줄 요약

**게임 코드는 한 줄도 안 바꿨다.** 매 세션 자동으로 로드되는 것들(규칙문서·스킬·MCP 커넥터)을 공식 문서 기준으로 진단해서
**7월 21일부터 규칙 파일 하나가 통째로 안 읽히고 있던 것**을 찾아 고치고, 무관한 짐을 걷어냈다.
작업 시작 전 `audit-parity.py`는 돌렸다 — **실제 영향 구멍 0개**(0804 상태 유지).

---

## 1. ⭐⭐ `@AGENTS.md`가 7/21부터 **죽은 줄**이었다 (이번 세션 제일 중요)

루트 `CLAUDE.md` 3번째 줄에 `@AGENTS.md`(=규칙 정본 1순위를 불러오는 줄)가 있었는데,

> **그 파일은 커밋 `6257731`(2026-07-21, "AI 도구 설정·작업 파일 저장소에서 제거")에서 삭제됐다.**

**없는 파일을 import하면 에러가 안 난다. 조용히 넘어간다.** → 7/21부터 지금까지 **"AI가 일하는 태도" 규칙 40줄이 매 세션 빠진 채** 돌고 있었다.

증거: 그 파일 §0-6에 이렇게 적혀 있었다 —

> *"성능·규칙 개선 요청이 오면 **새 규칙 추가가 기본이 아니다** — 무관 커넥터·플러그인·전역설정을 점검해 **스코프 교정·삭제로 수렴**한다."*

이번 세션에서 내가 이 결론을 **스스로 다시 도출했다.** 규칙이 로드됐다면 바로 따랐을 것 — 실제 손실이 있었다는 뜻.

### 확인 방법 (다음에 또 의심되면)

`/context` → **Memory files** 목록에 그 파일이 뜨는지 본다. 없으면 안 읽힌 것이다.

---

## 2. 고친 것 3가지

| # | 뭘 | 어디 |
|---|---|---|
| 1 | **죽은 import 제거 + 태도 규칙 흡수** (20줄 → 46줄) | `CLAUDE.md` |
| 2 | **무관 개인 스킬 15개 off** | `.claude/settings.local.json` → `skillOverrides` |
| 3 | **Figma MCP 완전 제거** (도구 45개) | `claude mcp remove TalkToFigma -s user` |

### 2-1. `CLAUDE.md` — 40줄을 그대로 되살리지 않고 **26줄로 줄여서** 넣었다

되살릴 때 **딴 데서 이미 강제되거나 중복인 것**을 전부 걷어냈다:

| 뺀 것 | 이유 |
|---|---|
| 작업일지 경로·스킬 라우팅표·에셋 소스·Pick 폴더·DebugMenuScene·PC별 경로 | **`myPokemon_AJ/AGENTS.md`에 이미 있다**(§3·§4·§6·§7·§8). 두 곳에 쓰면 한쪽만 고쳐져 모순난다 |
| "`Important/` 안 읽음" | permission이 이미 차단 |
| "커밋 시 tsc 훅 통과" | 훅이 이미 자동 차단 |
| **"커밋 전 `/verify` 필수"** | ⚠️ **그런 명령이 존재하지 않는다**(`.claude/commands/`·스킬 어디에도 없음). 없는 걸 시키는 규칙이었다 |
| "병렬 호출·서브에이전트·`/clear`" | 기본 동작 + `daily-session-cadence` 스킬에 있음 |

**새로 정한 것**: `/code-review` 게이트를 *"매 커밋 전 필수"*(0801부터 4일 연속 이월돼 사실상 사문화) 대신
→ **"하루 마무리 커밋(작업일지 쓰기) 전에 1회"**로 좁혔다. 지킬 수 있는 빈도로 낮춘 것.
(⚠️ 오늘은 **코드 변경이 0이라 해당 없음** — 리뷰할 diff가 없다.)

### 2-2. 스킬 15개 off — 토큰보다 **지침 충돌**이 이유

`docx·pptx·xlsx·pdf·resume-polisher·knowva-design-guard·assignment-quality-review·git-workflow-guard·portfolio-readme-upgrader·image-prompt-crafter·canvas-design·algorithmic-art·theme-factory·web-artifacts-builder·frontend-design`

특히 **`git-workflow-guard`는 "기본 브랜치 develop, develop에서 직접 커밋 금지"**를 강제하는데 **이 리포는 `main`에서 직접 작업한다.**
공식 문서: *"두 규칙이 충돌하면 Claude가 임의로 하나를 고른다."* → 정면 충돌하는 지침이 매 세션 붙어 있었다.

- **프로젝트+이 PC 한정.** 스킬 파일 자체는 `~/.claude/skills/`에 그대로 → 다른 프로젝트에선 정상 작동.
- **다음 세션부터 적용된다**(목록은 세션 시작 때 읽음). 확인은 `/clear` 후 `/skills`.

### 2-3. Figma — memory 기록이 현실과 달랐다

memory `workspace-audit-2026-07`엔 *"2026-07-08 전역 Figma 비활성화함"*이라고 돼 있었는데, **플러그인만 껐던 것**이고
**MCP 서버 등록(`~/.claude.json` → `mcpServers.TalkToFigma`)이 따로 살아서 `✔ Connected` 상태로 도구 45개를 계속 물고 있었다.**
→ 제거 후 `claude mcp list` = **비어 있음** 확인. memory 기록도 실제에 맞게 고쳤다.

> 교훈: **"껐다"는 기록을 믿지 말고 `claude mcp list`로 실물을 본다.** (= 0순위 계약 ①번 그 자체)

---

## 3. 측정 결과 (공식 문서 기준으로 실측)

| 항목 | 전 | 후 |
|---|---|---|
| 세션 시작 상시로드 | ~4,700 tok | **~3,000 tok** |
| MCP 도구 | 45개 | **0개** |
| 죽은 참조 | `@AGENTS.md` · `/verify` | **없음** |
| 규칙 충돌 | `git-workflow-guard`(develop 강제) | **없음** |

**결론: 용량 자체는 애초에 문제가 아니었다**(100만 창의 0.5% 미만, 공식 권고선 CLAUDE.md 200줄도 한참 밑돎).
진짜 문제는 **① 안 읽히는 규칙 ② 서로 모순되는 규칙 ③ 존재하지 않는 명령을 시키는 규칙** 세 개였고, 셋 다 없앴다.

잘 돼 있던 것(그대로 둠): `.claude/rules/*.md` 4개는 전부 `paths:` 조건부라 해당 파일 건드릴 때만 로드 · `MEMORY.md` 12줄(한도 200줄) · 스킬 본문 29KB는 발동될 때만.

---

## 4. ⚠️ 오늘 바꾼 건 **전부 이 PC에만 있다** (git에 안 올라감)

`.gitignore` 20~26줄이 **AI 설정을 통째로 제외**한다(`# 공개 저장소에서 제외한다`):

```
24: .claude/
25: CLAUDE.md      ← 오늘 고친 파일
26: AGENTS.md
```

- 그래서 `git status`가 **깨끗하다** — 오늘 커밋되는 건 **이 일지 파일 하나뿐**이다.
- `myPokemon_AJ/AGENTS.md` 하나만 추적되는 이유 = **이 gitignore 규칙이 생기기 전에 이미 추적 중이던 파일**이라 소급 적용이 안 된 것. 예외가 아니라 사고에 가깝다.
- **사용자 결정(2026-08-03): "그대로 둔다"** — `.gitignore`를 풀지 않는다. 공개 노출 방지 정책 유지.
- → **집 PC에는 이 규칙이 없다.** 거기서 작업할 일이 생기면 아래 이관 폴더에서 복사할 것.

### 이관 폴더 (git 동기화 안 되는 것 사본)

`C:\Users\ONE\Desktop\PokemonWith_이관_0805\` — `CLAUDE.md` · `settings.local.json` · memory 2개

---

## 5. 다음 세션 첫 프롬프트 (복붙용)

> "0805 일지 읽고 이어서. **0805는 규칙·컨텍스트 정비만 했고 게임 코드는 안 건드렸다** — `CLAUDE.md`의 죽은 `@AGENTS.md` import 제거+태도규칙 26줄 흡수 · 무관 스킬 15개 off · Figma MCP 제거. 게임 진행상태는 **0804 그대로**(audit-parity 실제 영향 구멍 0개).
>
> ⭐ **작업 시작 전에 `python3 tools/ar-map/audit-parity.py`부터 돌려라**(새 맵을 붙였으면 그 도구 `MAPS`에 한 줄 추가하는 것까지가 작업 완료다).
>
> 오늘 1순위 = **스토리 이벤트 아일라·경호**(0804 일지 §6.5에 원본 판독·트레이너 정의·배틀그림·이모트 조사까지 다 해뒀다 — 그걸 먼저 읽어라).
>  · 아일라(상록 23,35) = **오토런(trigger 3) 트레이너전** — WorldScene에 오토런 이벤트 + 이벤트에서 배틀 걸기가 필요하다. 지면 이벤트 종료(재도전 가능).
>  · 경호(상록 35,10) = **207(이모트 말풍선)만 처리하면** 지금 이벤트 파이프라인으로 그대로 돈다. 이모트는 AR 애니 3번(!)·4번(?)이고 우리에게 플레이어·데이터가 이미 있다.
>  · ⛔ **1번도로 오박사 조우는 손대지 마라** — 원본은 그게 게임 오프닝(도로에서 기억 잃고 깨어남 → 연구소 강제이동)이라 사용자가 직접 쓴 우리 오프닝과 충돌한다. **사용자가 ①제외 ②각색 ③교체 중 고르기 전까진 착수 금지.**
>
> 2순위 = 포켓몬 따라다니기(태초 EV011). 3순위 = 타운맵. 4순위 = 상점(마트·민가4 젬).
>
> ⚠️ 함정(0804에서 이월): ① `public/`에 새 파일 만들면 **dev 서버 재시작** ② 부팅 직후 첫 씬은 `getScenes(true)`에 안 잡힌다 ③ **검증에서 플레이어를 벽 위로 순간이동시키지 말 것**(설 자리 `walkable()` 먼저 확인) ④ **열매나무 시트는 세로 4줄이 방향이 아니라 성장단계**다 ⑤ `import-from-anotherred.mjs trainers`는 338장을 통째로 가져온다 → 쓰는 것만 남길 것 ⑥ `extract-trainers.py`는 `--trainers`에 **기존 것까지 다 적어야** 안 날아간다.
> ⚠️ **가방 스크롤 화살표는 아직 미해결. 임의로 색칠하지 말 것**(0803 §7).
> ⚠️ 0805 신설: **작업일지 쓰기 전에 `/code-review` 1회**(매 커밋마다가 아니라 하루 마무리에 한 번).
> **한 블록 끝나면 일지 쓰고 `/clear`.** 사용자에게 보여줄 사진은 `01_Resources/Pick/`. 규칙 함부로 추가 금지. 창 띄우기 전 먼저 물어볼 것."
