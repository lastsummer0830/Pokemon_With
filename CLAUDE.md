# Pokemon_With — Claude Code 진입점

이 파일은 세션을 어디서 시작하든 매번 전문 로드된다. 세션 단위 계약만 담고,
게임 파일을 만질 때 필요한 것은 `myPokemon_AJ/AGENTS.md`가 그때 들고 온다. 같은 내용을 두 곳에 쓰지 않는다.

## 역할·권위
- Claude는 Hermes가 정한 저장소 내부 계약 안에서 현재 코드 조사, 구현 설계, 코드 변경, 자체 실행·검증을 담당하는 개발자다. Hermes는 범위·승인·독립 재검증·사용자 보고를 담당한다.
- 사용자 최신 지시를 우선하고, 현재 파일·Git·실행 결과로 사실을 확인한다. 과거 문서·기록보다 실물을 믿는다.
- Hermes 제어면을 다룰 때만 저장소의 `.hermes.md`를 직접 읽어 권위·안전 규칙을 적용한다. 하위 CWD 승인창을 막기 위해 자동 import하지 않는다.

## 안전·승인
- 기존 변경을 reset·restore·clean·덮어쓰기하지 않는다.
- 삭제, 대량 이동, 패키지 설치, commit, push, 배포는 사용자 승인 후 한다. 아래 세션 마무리 흐름만 예외다.
- 완료를 말하기 전에 실제로 실행해 확인한다. 확인하지 못한 항목은 `미검증`과 이유를 남긴다.

## 작업일지 · 세션 마무리
> 2026-07-22 사용자 지시. 목적은 기록 보존이 아니라 **다음 세션이 안전하게 이어받는 것**이다.

- "작업일지 작성"(동일 의도 포함)이면 되묻지 말고 순서대로 한다.
  **① 코드 변경이 있었으면 `/code-review` 1회 → ② 일지 작성 → ③ 커밋 → ④ push → ⑤ 다음 세션 첫 프롬프트 출력**
- **"작업일지 써"는 커밋·push까지 포함한 지시다.** 이 흐름에 한해 위의 commit·push 승인 조항을 적용하지 않는다. 이 흐름 밖의 커밋·push는 여전히 승인이 필요하다.
- 위치는 `<repo-root>/00_ImportBox/작업일지/`, 이름은 `MMDD_PokemonWith_작업일지.md` 한 파일이다. 날짜 하위폴더를 만들지 않는다. 저장소 루트는 `git rev-parse --show-toplevel`으로 구하고 절대경로를 박지 않는다. 같은 날 파일이 있으면 덮지 말고 이어 보강한다.
- **분량 상한 80줄·6KB.** 넘으면 과정 서술을 자르고 결론만 남긴다. 긴 조사·수치·캡처는 `.claude/.verify/`나 memory에 두고 일지는 그 경로만 가리킨다.
- 다섯 항목만 쓴다. ① 한 것(파일 경로까지) ② 검증 결과 ③ 미검증·남은 위험 ④ **다음 세션 시작 지점**(열 파일·첫 명령) ⑤ 그날 바뀐 지침·Skill 요지.
- 사용자에게 보여줄 캡처·후보 이미지는 `01_Resources/Pick/<카테고리>/`에 두고 일지에 그 경로를 적는다. 자동 검증 캡처(`.claude/.verify/`)와 섞지 않는다.
- ⑤는 일지에 적는 것만으로 부족하다. 사용자가 `/clear` 직후 그대로 붙여넣도록 **채팅 마지막에 복붙 가능한 한 덩어리**로 낸다.
- git으로 동기화되지 않는 것만 현재 PC 바탕화면 `PokemonWith_이관_MMDD/`에 사본을 두고 그 경로를 알린다. 저장소 안에 이관 폴더를 만들지 않는다.

## 언제 마무리하나
- 기준은 컨텍스트 잔량이 아니라 **작업 경계**다. 검증까지 끝난 단위 하나가 끝난 시점에 마무리를 권한다. 어중간하게 끊으면 일지에 미검증 항목만 쌓인다.
- 백스톱으로 `/context`가 **60%**를 넘으면 새 단위를 시작하지 않고 그 세션을 마무리한다. 남은 여유는 검증과 일지 품질에 쓴다.
- 마무리를 권할 때는 위 작업일지 절차를 그대로 타고, 끝에 `/clear` 권고와 다음 세션 첫 프롬프트를 함께 낸다. 압축된 세션은 규칙이 온전하지 않으므로(아래 로드 표 참고) 길게 끌기보다 새로 시작하는 편이 낫다.

## 진행상태는 어디 있나
- **정본은 작업일지다.** git으로 동기화되므로 다른 PC에서도 읽힌다. "지금 어디까지 했나"는 최신 일지를 먼저 읽어 판단한다.
- auto memory(`MEMORY.md`와 topic 노트)는 **이 PC 캐시**다. PC 간 공유되지 않으므로, 다음 세션이 반드시 알아야 하는 것은 memory에만 두지 않고 일지에 남긴다.
- 진행상태를 이 파일이나 `AGENTS.md`에 고정 서술하지 않는다. 규칙 문서에 상태를 적으면 낡는다.

## Skill 사용
- Skill 목록(이름·설명)은 세션 시작 때 이미 로드돼 있다. 작업 의도가 목록의 설명과 맞으면 **바로 코딩하지 말고 해당 Skill을 먼저 발동**해 그 절차와 함정을 따른다.
- 경계가 헷갈리는 셋: **원본과 비교·재현 검증 = `ar-compare`** / 소스 후보 수집·이식 = `pokemon-asset-pipeline` / 실제 동작 확인 = `webapp-testing`.
- 공식 `/run`, `/verify`, `/code-review`, `/debug`도 쓸 수 있으나, 그 결과가 프로젝트 acceptance와 Hermes 독립 검증을 대체하지 않는다.
- `.agents/skills`가 Skill 정본이고 `.claude/skills`는 `python3 scripts/check_agent_skills.py --sync --prune`로 만드는 동일 mirror다. mirror를 직접 편집하지 않는다.

## 규칙이 어디서 로드되나
| 파일 | 로드 시점 |
|---|---|
| 이 파일 | 매 세션 전문 |
| `myPokemon_AJ/AGENTS.md` | 그 폴더 파일을 읽을 때 (`myPokemon_AJ/CLAUDE.md`가 import) |
| `.claude/rules/*.md` 4개 | `paths:` 매칭 파일을 읽을 때 |
| Skill 본문 | 그 Skill을 발동할 때 |

- 하위 `CLAUDE.md`와 `paths:` 규칙은 `/compact` 후 재주입되지 않는다. 압축 뒤 게임 파일을 계속 만진다면 해당 규칙을 다시 읽는다.
- 로드 여부가 의심되면 `/context`의 **Memory files** 목록으로 확인한다. 없는 파일을 import해도 에러가 나지 않고 조용히 넘어가므로, 기록이 아니라 목록을 본다.
- hook은 `.claude/settings*.json`에 현재 등록된 항목만 활성으로 판단한다. 스크립트가 존재한다는 이유만으로 실행하거나 활성이라고 가정하지 않는다.
- `myPokemon_AJ`에서 시작하는 세션은 `/add-dir ..`, 단발 실행은 `--add-dir ..`로 저장소 상위를 먼저 추가해 루트 `.claude/.verify` 캡처를 읽을 수 있게 한다.

## 규칙을 고칠 때
- 성능·규칙 개선 요청에 **새 규칙 추가가 기본이 아니다.** 안 읽히는 규칙, 서로 모순되는 규칙, 존재하지 않는 명령을 시키는 규칙부터 찾아 없앤다.
- 규칙·Skill·설정을 작업 도중 임의로 바꾸지 않는다. 변경안을 설명하고 승인받은 뒤 적용한다.
- 제어면 계약은 `.claude/settings.json`·`scripts/check_hermes_control.py`·`scripts/test_agent_skills.py` 세 곳에 복제돼 있다. 하나만 고치면 검사가 깨지므로 함께 고치고 `python3 scripts/check_hermes_control.py`로 확인한다.
- `.claude/settings.json`의 권한 변경은 진행 중인 세션에 반영되지 않는다. 새 세션에서 확인한다.
